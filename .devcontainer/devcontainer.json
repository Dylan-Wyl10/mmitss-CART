{
    "name": "MMITSS Dev Container",
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },
    "mounts": [
        "source=${env:HOME}/RA/MMITSS_dev/nojournal,target=/nojournal,type=bind"
    ],
    "runArgs": [
    "--network=host"
    ]
}
